version: '3.9'
services:
  backend:
    build: ./backend
    environment:
      - PORT=4000
    ports:
      - '4000:4000'
  frontend:
    build: ./frontend
    environment:
      - VITE_API_BASE_URL=http://backend:4000
    ports:
      - '5173:4173'
    depends_on:
      - backend
